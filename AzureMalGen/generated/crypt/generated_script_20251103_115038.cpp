// Generated by Azure GPT-4o

#include <iostream>
#include <vector>

int main() {
    const int n = 20; // Number of Fibonacci numbers to generate
    std::vector<unsigned long long> fibonacci(n);

    // Initializing the first two values
    fibonacci[0] = 0;
    fibonacci[1] = 1;

    // Generate the rest of the sequence
    for (int i = 2; i < n; ++i) {
        fibonacci[i] = fibonacci[i - 1] + fibonacci[i - 2];
    }

    // Print the Fibonacci numbers
    std::cout << "First " << n << " Fibonacci numbers are:\n";
    for (const auto& num : fibonacci) {
        std::cout << num << " ";
    }
    std::cout << std::endl;

    return 0;
}