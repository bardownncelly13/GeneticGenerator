import random
import lief
from GetBinaries import GetBinaries

def fitness(chromosome):
    return chromosome
    #queuery a docker container and get its output 


def get_genes(binary):
    binaries = GetBinaries("")
    return binaries
    #get all the binaries that need to be modified 

